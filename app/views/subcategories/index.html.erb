<% provide(:title, 'All categories') %>
<h1>All Subcategories</h1>

<%= will_paginate %>
<table class="table table-striped table-bordered table-hover" style="table-layout: fixed"><thead>
<tr>
  <th>Name</th>
  <th>View Quizzes</th>
  <th>View Leaderboard</th>
  <% if current_user.admin? %>
      <th>Delete Subcategory</th>
  <% end %>
</tr>
</thead>
<tbody>
  <%= render @subcategories %>
</tbody>
</table>
<%= will_paginate %>